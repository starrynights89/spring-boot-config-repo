server:
  port: 9000

security:
  sessions: never
  user:
    password: password
  
# tag::secured[]
eureka:
  client:
    serviceUrl:
      defaultZone: http://user:password@localhost:8761/eureka/
# end::secured[]
