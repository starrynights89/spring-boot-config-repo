server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: https://spring-social-eureka-server.cfapps.io/eureka/
  instance:
    hostname: ${vcap.application.uris[0]}
    nonSecurePort: 80
