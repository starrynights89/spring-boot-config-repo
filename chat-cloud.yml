server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka.credentials.uri}
  instance:
    hostname: ${vcap.application.uris[0]}
    secure-port: 433
    secure-port-enabled: true
