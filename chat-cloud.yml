server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: https://spring-social-eureka-server.cfapps.io/eureka/
  instance:
    hostname: ${vcap.application.uris[0]:localhost:8761/eureka/}
    secure-port: 433
    secure-port-enabled: true
