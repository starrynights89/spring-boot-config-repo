server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: https://spring-social-eureka-server.cfapps.io
  instance:
    hostname: ${vcap.application.uris[0]:localhost}
    nonSecurePort: 80