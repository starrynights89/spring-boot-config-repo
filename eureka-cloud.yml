server:
  port: 8080
  
logging:
  level:
    org.springframework.cloud: 'DEBUG'
    
eureka:
  instance: 
    hostname: ${vcap.application.uris[0]}
    secure-port: 433
    secure-port-enabled: true
